# cURL 示例分析报告

## 当前状态

**cURL 示例数量：** 4 条  
**案例库案例数量：** 8 个 JSON 文件

## 当前 4 条示例的作用

1. **#1 基础信息查询** - 展示基础用法（ONLY_SUGGEST 决策）
2. **#2 保证收益拒答** - 展示拒绝场景（DENY 决策）
3. **#3 高额退款 HITL** - 展示 context 参数和 HITL 场景
4. **#4 Verbose 模式** - 展示调试功能（verbose 参数）

## 分析

### ✅ 优点
- **覆盖主要决策类型**：ONLY_SUGGEST、DENY、HITL
- **展示关键参数**：`debug`、`verbose`、`context`
- **简洁实用**：快速开始部分应该简洁，避免信息过载
- **与案例库互补**：详细案例在"案例库"部分，cURL 示例用于快速上手

### ⚠️ 可以改进的地方
- **#3 和 #4 都使用 context**：可以合并或调整
- **缺少 tool_id 示例**：Case 7 展示了 `tool_id` 的使用
- **缺少 order_id 示例**：Case 5 展示了 `order_id` 的使用
- **缺少 session_id 示例**：Case 4（多轮对话）展示了 `session_id` 的使用

## 建议方案

### 方案 1：保持现状（推荐）
**理由：**
- 4 条示例已经覆盖主要使用场景
- "快速开始"部分应该简洁
- 详细用法在"案例库"和"API 文档"部分有完整说明

### 方案 2：优化现有示例
**调整建议：**
- 合并 #3 和 #4，因为都使用 context
- 补充一条展示 `tool_id` 或 `order_id` 的使用
- 保持 4-5 条示例

### 方案 3：补充到 6-7 条
**补充内容：**
- Case 5: 地址变更（展示 order_id）
- Case 7: 证据缺失（展示 tool_id）
- Case 8: 冲突证据（展示完整 context）

## 推荐方案

**建议采用方案 1（保持现状）+ 小幅优化：**

1. **保持 4 条示例**（不多余）
2. **优化 #3 和 #4**：
   - #3 保留，展示 context + amount
   - #4 改为展示 tool_id 或 order_id，保留 verbose
3. **添加说明**：在示例后添加"更多示例请查看[案例库](#案例库)部分"

## 结论

**当前 4 条示例：**
- ✅ **不多余**：每条都有明确目的
- ✅ **覆盖主要场景**：基础查询、拒绝、HITL、调试
- ⚠️ **可以优化**：补充 context 参数的更多用法示例

**建议：**
- 保持 4-5 条示例
- 优化示例内容，展示更多 context 参数用法
- 在示例后添加指向案例库的链接
